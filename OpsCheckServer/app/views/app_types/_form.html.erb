<%= simple_form_for(@app_type) do |f| %>
  <%= f.error_notification %>

  <div class="inputs form-group">
    <%= f.input :name %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
